{% extends 'base_clean.html.twig' %}

{% block title %}Neuer BaseType - Administration - {{ parent() }}{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1><i class="bi bi-diagram-3 me-2"></i>Neuer BaseType</h1>
        <p class="text-muted mb-0">Erstellen Sie einen neuen Basis-Typ für Onboarding-Templates</p>
    </div>
    <a href="{{ path('app_admin_base_types') }}" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>Zurück zur Übersicht
    </a>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i>BaseType Details</h5>
            </div>
            <div class="card-body">
                <form method="POST">
                
                <div class="mb-3">
                    <label for="name" class="form-label">Name</label>
                    <input type="text" class="form-control" id="name" name="name" placeholder="Geben Sie den Namen des BaseTypes ein..." required>
                    <div class="form-text">
                        Der Name des BaseTypes, z.B. "Standard Onboarding" oder "IT-Mitarbeiter"
                    </div>
                </div>

                <div class="mb-3">
                    <label for="description" class="form-label">Beschreibung</label>
                    <textarea class="form-control" id="description" name="description" rows="4" placeholder="Beschreiben Sie den Zweck und Inhalt dieses BaseTypes..."></textarea>
                    <div class="form-text">
                        Eine detaillierte Beschreibung des BaseTypes und wofür er verwendet wird
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-1"></i>BaseType erstellen
                    </button>
                    <a href="{{ path('app_admin_base_types') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-x-circle me-1"></i>Abbrechen
                    </a>
                </div>

                </form>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-header">
                <h6 class="mb-0"><i class="bi bi-info-circle me-1"></i>Hinweise</h6>
            </div>
            <div class="card-body">
                <h6>Was ist ein BaseType?</h6>
                <p class="small">BaseTypes sind Basis-Vorlagen für Onboarding-Prozesse. Sie enthalten Task-Blöcke und Aufgaben, die als Template für verschiedene OnboardingTypes verwendet werden können.</p>
                
                <h6>Verwendung</h6>
                <ul class="small">
                    <li>Erstellen Sie wiederverwenDBare Onboarding-Templates</li>
                    <li>Definieren Sie Standard-Aufgaben für bestimmte Rollen</li>
                    <li>Änderungen am BaseType wirken sich auf alle abgeleiteten OnboardingTypes aus</li>
                </ul>

                <div class="alert alert-warning small">
                    <i class="bi bi-exclamation-triangle me-1"></i>
                    <strong>Wichtig:</strong> Nach dem Erstellen können Sie Task-Blöcke und Aufgaben über die Administration hinzufügen.
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
